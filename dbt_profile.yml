# https://docs.getdbt.com/docs/profile

otus_tutorials:
  outputs:
    dev:
      type: postgres
      threads: 1
      host: postgres
      port: 5432
      user: airflow
      pass: airflow
      dbname: airflow
      schema: public
    prod:
      type: postgres
      threads: 1
      host: postgres
      port: 5432
      user: airflow
      pass: airflow
      dbname: airflow
      schema: public
  target: dev
